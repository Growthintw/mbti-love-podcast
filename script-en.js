const slides=[...document.querySelectorAll('.slogans h1')];const dots=[...document.querySelectorAll('.dots button')];let current=0;function show(i){slides[current].classList.remove('active');dots[current].classList.remove('active');current=i;slides[current].classList.add('active');dots[current].classList.add('active')}dots.forEach((d,i)=>d.addEventListener('click',()=>show(i)));setInterval(()=>show((current+1)%slides.length),4800);
const topics=[['In the adult world, do we filter instead of teach?','When someone does not meet our expectations, should we grow with them, or admit we are not right for each other?'],['Do different life stages make us fall for different people?','When work, dreams, or family become the center of life, does the partner we choose quietly change too?'],['If someone checks every box but cannot make you laugh, how many points remain?','If a person is nearly perfect on paper but there is no spark in conversation, how would you score the relationship?'],['In love, do you want growth or happiness more?','Do you expect a partner to push you into becoming better, or to let you feel safe being your original self?'],['Have you ever changed yourself for love until you were no longer happy?','Was there a relationship where you tried hard to become what someone wanted, only to drift farther away from yourself?'],['Do you want emotional stability because you want to be held?','When you are tired or vulnerable, do you expect your partner to care for your emotions, or do you hope each person stays responsible for themselves?'],['Beyond appearance, what part of yourself are you most proud of?','If you could not talk about looks, what trait would help someone you like see your value?'],['What was the most difficult adjustment you made in a relationship?','What did you both do, and did it eventually work? If it did, what helped the relationship survive; if not, where did it get stuck?']];document.querySelector('#topicGrid').innerHTML=topics.map((t,i)=>`<article><span>0${i+1}</span><h3>${t[0]}</h3><p>${t[1]}</p></article>`).join('');

document.querySelectorAll('.dimension-card').forEach(card=>{const toggle=()=>{const flipped=card.classList.toggle('is-flipped');card.setAttribute('aria-pressed',String(flipped))};card.addEventListener('click',toggle);card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();toggle()}})});

document.querySelectorAll('.scratch-card').forEach(card=>{const canvas=card.querySelector('.scratch-layer');const button=card.querySelector('.scratch-reveal');const ctx=canvas.getContext('2d',{willReadFrequently:true});let drawing=false,last=null,revealed=false;const paint=()=>{if(revealed)return;const rect=card.getBoundingClientRect();const dpr=Math.min(window.devicePixelRatio||1,2);canvas.width=Math.round(rect.width*dpr);canvas.height=Math.round(rect.height*dpr);canvas.style.width=rect.width+'px';canvas.style.height=rect.height+'px';ctx.setTransform(dpr,0,0,dpr,0,0);ctx.globalCompositeOperation='source-over';ctx.fillStyle=card.dataset.color;ctx.fillRect(0,0,rect.width,rect.height);ctx.fillStyle='rgba(255,255,255,.72)';ctx.font='800 12px Inter,system-ui,sans-serif';ctx.textAlign='left';ctx.fillText(card.dataset.kicker,32,44);ctx.fillStyle='#fff';ctx.font='800 27px Inter,system-ui,sans-serif';ctx.fillText(card.dataset.lineOne,32,150);ctx.fillText(card.dataset.lineTwo,32,194);ctx.fillStyle='rgba(255,255,255,.78)';ctx.font='700 13px Inter,system-ui,sans-serif';ctx.fillText('Scratch with finger or mouse',32,rect.height-34)};const point=e=>{const r=canvas.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top}};const scratch=(from,to)=>{ctx.globalCompositeOperation='destination-out';ctx.lineCap='round';ctx.lineJoin='round';ctx.lineWidth=46;ctx.beginPath();ctx.moveTo(from.x,from.y);ctx.lineTo(to.x,to.y);ctx.stroke()};const reveal=()=>{revealed=true;card.classList.add('is-revealed')};const check=()=>{const data=ctx.getImageData(0,0,canvas.width,canvas.height).data;let clear=0,total=0;for(let i=3;i<data.length;i+=64){total++;if(data[i]<40)clear++}if(clear/total>.42)reveal()};canvas.addEventListener('pointerdown',e=>{drawing=true;last=point(e);canvas.setPointerCapture(e.pointerId);scratch(last,last)});canvas.addEventListener('pointermove',e=>{if(!drawing)return;const next=point(e);scratch(last,next);last=next});const stop=()=>{if(!drawing)return;drawing=false;last=null;check()};canvas.addEventListener('pointerup',stop);canvas.addEventListener('pointercancel',stop);button.addEventListener('click',reveal);new ResizeObserver(paint).observe(card);paint()});

document.querySelectorAll('.coffee-stage').forEach(stage=>stage.addEventListener('click',()=>{const card=stage.closest('.coffee-card');if(!card.classList.contains('is-revealed'))return;card.classList.add('show-qr');stage.setAttribute('aria-pressed','true');stage.setAttribute('aria-label','Coffee coupon QR Code is now visible')}));
